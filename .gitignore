# OS
.DS_Store
**/.DS_Store

# Secrets / env
.env
.env.local
.env*.local

# Prep material & assets — not for GitHub
docs/
design/
idea.md
mascot-generation/
generated/
resource*/

# Local build progress log — not for GitHub
BUILD_LOG.md

# Node / build (future app)
node_modules/
.next/
dist/
build/

# Vendored external skill (cloned, not for our repo) + local agent config
vendor/
.claude/
.windsurf/


ARCHITECTURE-SOLID.md

PROVIDERS.md
# Runtime data (ingested materials, podcasts) — not for GitHub
data/

# Python worker artifacts
__pycache__/
*.pyc

# TS incremental build artifact
tsconfig.tsbuildinfo
